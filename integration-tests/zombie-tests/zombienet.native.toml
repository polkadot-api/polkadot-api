[relaychain]
default_command = "polkadot"
default_args = ["-lparachain=debug", "--state-pruning=archive-canonical"]

chain = "rococo-local"

[[relaychain.nodes]]
name = "alice"
validator = true
rpc_port = 9933

[[relaychain.nodes]]
name = "bob"
rpc_port = 9934
validator = true
