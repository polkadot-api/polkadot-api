[relaychain]
default_command = "polkadot"
default_args = [ "-lparachain=debug", "--network-backend=litep2p"]

chain = "westend-local"

  [[relaychain.nodes]]
  name = "alice"
  validator = true
  rpc_port = 9933

  [[relaychain.nodes]]
  name = "bob"
  rpc_port = 9934
  validator = true
