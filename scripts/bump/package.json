{
  "name": "papi-bump-changelog",
  "private": true,
  "devDependencies": {
    "@types/bun": "^1.3.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "release": "bun run ./propagate-update.ts && bun run ./release.ts && bun run ./merge-update-dependencies.ts"
  }
}
