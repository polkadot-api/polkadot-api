# Polkadot API docs generator

This package generates chain-specific documentation, based on metadata.

Usage:

Run the script in directory that contains `.papi`

```
npx papi-generate-docs --output docs
```
