{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "inputs": ["tsconfig.build.json"],
      "outputs": ["bin/**"]
    },
    "test": {
      "inputs": ["vitest.config.ts"]
    }
  }
}
